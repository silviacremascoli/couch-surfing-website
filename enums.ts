export enum Permissions {
    ADMIN = 'ADMIN',
    READ_ONLY = 'READ_ONLY'
}

export enum LoyaltyUsers {
    GOLD_USER = "GOLD_USER",
    SILVER_USER = "SILVER_USER",
    BRONZE_USER = "BRONZE_USER"
}